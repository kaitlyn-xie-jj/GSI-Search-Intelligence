

# BEGIN GSI SearchWorld V1 profile (managed).
add_sitl_profile \
    --id "GSI SearchWorld V1" \
    --name "GSI parameterized outdoor search benchmark" \
    --world "gsi_search_world_v1" \
    --target "gz_q940_ti_gripper4_gsi_search_world_v1" \
    --pose "0,0,0.1,0,0,0"
# END GSI SearchWorld V1 profile (managed).
